"use strict";
/**
 * ---------------------------------------------------------
 * Creating the first variable
 * ---------------------------------------------------------
 */
let message;
message = 'Hello'; // the string  was storage in the memory
console.log(message); // Hello

let newMessage = 'Hello, World';
console.log(newMessage);

/**
 * --------------------------------------------------------- 
 * We can also declare multiple variables in one line
 * --------------------------------------------------------- * 
 */

let user = 'John', age = 25, personalMessage = 'Hi' // We don't recomend it. Use multiple lines in order to define your variables
